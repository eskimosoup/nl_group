<h2><%= team_member_team_presenter.name %></h2>

<%= render partial: 'team_members/team_member', collection: BaseCollectionPresenter.new(collection: team_member_team_presenter.team_members, view_template: self, presenter: TeamMemberPresenter), as: :team_member_presenter %>
