<div class="testimonial-slide-<%= testimonial_presenter.id %>">
  <div class="row collapse small-uncollapse">
    <!--
    <div class="medium-2 columns">
      <% if testimonial_presenter.video_embed_code.present? && testimonial_presenter.show_image.present? %>
        <div class="testimonial-image">
          <%= link_to (image_tag 'components/icons/play-icon.png', alt: 'Play video', class: 'testimonial-play-video'), '#',
                      title: testimonial_presenter.name, class: "css-animated-hover testimonial-video-link-#{testimonial_presenter.id}",
                      data: { class: 'tada' } %>
          <%= testimonial_presenter.show_image(alt: testimonial_presenter.name, class: 'testimonial-thumbnail') %>
        </div>
      <% elsif testimonial_presenter.video_embed_code.blank? && testimonial_presenter.show_image.present? %>
        <div class="testimonial-image">
          <%= testimonial_presenter.show_image(alt: testimonial_presenter.name, class: 'testimonial-thumbnail') %>
        </div>
      <% end %>
    </div>
    -->

    <div class="medium-12 columns">
      <%= image_tag 'components/content-boxes/testimonial-open-quote.png', alt: '', class: 'testimonial-quote-mark' %>

      <div class="testimonial-content">
        <%= testimonial_presenter.content %>
      </div>

      <div class="testimonial-author">
        <%= testimonial_presenter.name %>, <%= testimonial_presenter.role %>
      </div>

      <%#= link_to "Watch Video", '#', class: "testimonial-watch-video-link testimonial-video-link-#{testimonial_presenter.id}", title: testimonial_presenter.name  %>

      <div class="video hide">
        <div id="testimonial-video-<%= testimonial_presenter.id %>" class="testimonial-video-wrap">
          <!--
          <div class="flex-video">
            <%= testimonial_presenter.video_embed_code %>
          </div>
          -->
        </div>
      </div>
    </div>
  </div>
</div>
